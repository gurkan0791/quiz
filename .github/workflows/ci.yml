name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check executable
        run: chmod +x ../hello.sh

      - name: run script
        run: ./../hello.sh